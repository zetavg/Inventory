//
// DO NOT edit this file directly. The content of this file is auto generated
// by `yarn run copy-dependencies.`
//
// Please edit the following source file instead:
//   packages/data-storage-couchdb/lib/functions/getUpdateConfig.ts
//

import { UpdateConfig } from '@deps/data/types';

import getGetConfig, { CONFIG_ID } from './getGetConfig';
import { Context } from './types';

export default function getUpdateConfig(context: Context): UpdateConfig {
  const { db, dbType, logger, logLevels } = context;

  const getConfig = getGetConfig(context);

  const updateConfig: UpdateConfig = async config => {
    const newConfig = {
      _id: CONFIG_ID,
      ...(await getConfig()),
      ...config,
    };
    if (dbType === 'pouchdb') {
      await db.put(newConfig);
    } else {
      await db.insert(newConfig);
    }

    return newConfig;
  };

  return updateConfig;
}
